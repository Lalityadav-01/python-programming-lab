# The program to check whether given number is positive or negative.
n = int(input ("Enter the number:"))

if n > 0:

              print ("The number provided is positive")

elif n == 0:          

              print ("The number input is zero")

else:

              print ("The number provided is negative")
